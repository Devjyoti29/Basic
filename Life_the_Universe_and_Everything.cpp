#include <bits/stdc++.h>
#include<queue>
using namespace std;
int main()
{
   int n;
   n=getchar();
   while(n!=EOF)
   {
         if(n!=(int)('*'))
          putchar(n);
        n=getchar();
       
    }
    return 0;
}